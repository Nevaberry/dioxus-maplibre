{
  "name": "dioxus-maplibre-e2e",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "copy-fixtures": "bun run scripts/copy-fixtures.ts",
    "serve": "bun run src/server.ts",
    "test": "bunx playwright test",
    "test:headed": "bunx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "pixelmatch": "^5.3.0",
    "pngjs": "^6.0.0",
    "@types/pngjs": "^6.0.0",
    "glob": "^11.0.0"
  }
}
